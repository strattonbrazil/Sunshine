#ifndef SUNSHINEUI_H
#define SUNSHINEUI_H

/*
#include <QMainWindow>
#include "scene.h"

class SunshineUi : public QMainWindow
{
    Q_OBJECT
public:
    explicit SunshineUi(Scene* scene, QWidget *parent = 0);
    SunshineUi(const SunshineUi &ui);
    Scene* scene() const { return _scene; }
private:
    Scene* _scene;
signals:

public slots:

};
*/
#endif // SUNSHINEUI_H
